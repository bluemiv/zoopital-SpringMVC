<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pet.model.AdminDAO">
	 <insert id="insert" parameterType="com.pet.model.AdminDTO">
	    INSERT INTO ADMIN
	    	(ADMIN_ID, ADMIN_PWD, ADMIN_ENABLED, ADMIN_ROLE, STORE_CODE)
	    VALUES
	    	(#{admin_id}, #{admin_pwd}, #{admin_enabled}, #{admin_role}, #{store_code})
    </insert>
    <update id="update" parameterType="com.pet.model.AdminDTO">
    	UPDATE ADMIN
    	<set>
    		<if test="admin_id != null">
    			ADMIN_ID = #{admin_id},
    		</if>
    		<if test="admin_pwd != null">
    			ADMIN_PWD = #{admin_pwd},
    		</if>
    		<if test="admin_enabled != null">
    			ADMIN_ENABLED = #{admin_enabled},
    		</if>
    		<if test="admin_role != null">
    			ADMIN_ROLE = #{admin_role},
    		</if>
    		<if test="store_code != null">
    			STORE_CODE = #{store_code}
    		</if>
    	</set>
    	WHERE STORE_CODE = #{store_code}
    </update>
</mapper>